# Build output — regenerated by `mkdocs build`; never commit it.
site/
public/

# Local Python environment
.venv/
venv/
__pycache__/
*.pyc

# Editor / OS noise
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# LibreOffice leftovers from `python scripts/build_forms.py` (the PDF conversion
# step). Harmless, but they land in docs/forms/ on every run.
.~lock.*#
*.tmp
